{
  "name": "mcp-box",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @mcp-box/mcp-web dev",
    "dev:legacy": "cd mcp-box-web && yarn dev",
    "build": "yarn workspaces run build",
    "build:legacy": "cd mcp-box-web && yarn build",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
